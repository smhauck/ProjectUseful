<%= form_for(@meeting_assignment) do |f| %>
  <% if @meeting_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting_assignment.errors.count, "error") %> prohibited this meeting_assignment from being saved:</h2>

      <ul>
      <% @meeting_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meeting_id %><br>
    <%= f.text_field :meeting_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
