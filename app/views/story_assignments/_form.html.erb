<%= form_for(@story_assignment) do |f| %>
  <% if @story_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story_assignment.errors.count, "error") %> prohibited this story_assignment from being saved:</h2>

      <ul>
      <% @story_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :story_id %><br>
    <%= f.text_field :story_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
