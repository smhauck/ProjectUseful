<%= form_with(model: email_type, local: true) do |form| %>
  <% if email_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email_type.errors.count, "error") %> prohibited this email_type from being saved:</h2>

      <ul>
      <% email_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :email_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :email_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
