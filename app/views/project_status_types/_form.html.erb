<%= form_for(@project_status_type) do |f| %>
  <% if @project_status_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_status_type.errors.count, "error") %> prohibited this project_status_type from being saved:</h2>

      <ul>
      <% @project_status_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
