<%= form_for(@email_to_sms_gateway) do |f| %>
  <% if @email_to_sms_gateway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_to_sms_gateway.errors.count, "error") %> prohibited this email_to_sms_gateway from being saved:</h2>

      <ul>
      <% @email_to_sms_gateway.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
